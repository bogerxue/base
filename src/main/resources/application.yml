server:
  port: 10086


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///person_project?serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: root


logging:
  file:
    path: F:\\xiangmulog
    max-size: 100MB
  level:
    top:
      wenbovo:
        cn: debug
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl